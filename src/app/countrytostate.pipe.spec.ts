import { CountrytostatePipe } from './countrytostate.pipe';

describe('CountrytostatePipe', () => {
  it('create an instance', () => {
    const pipe = new CountrytostatePipe();
    expect(pipe).toBeTruthy();
  });
});
